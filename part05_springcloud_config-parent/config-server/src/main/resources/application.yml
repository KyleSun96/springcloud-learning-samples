server:
  port: 9527

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: config-server
  # spring cloud config
  cloud:
    config:
      server:
        git:         # git 的 远程仓库地址
          uri: https://gitee.com/kylesun96/config.git
      label: master  # 分支配置